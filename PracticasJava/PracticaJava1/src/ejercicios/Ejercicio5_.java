package ejercicios;

public class Ejercicio5_ {
/*
    public static void main(String[] args) {

        int left = 0;
        String right = "";
        int left_d_quantity;
        int count = 0;

        while(count<n) {
            right = Integer.toString(left) + right;
            left_d_quantity = countMatches(Integer.toString(left, Integer.toString(d)));
            if(left_d_quantity>=1){
                for (int i = 0; i<10 && count<n; ){
                    left
                    System.out.println(left+right);
                    count++;
                }
                if(count==n)
                    break;
                continue;
            }

            for (int i = 0; i<) {

                if (){
                    System.out.println(left+right);
                    count++;
                }
            }
            left++;

        }
        "7"
        "17" "27" "37" "47" "77" "87"... "107" "117".. "167" "170" "171...179" "187"


    }

    private static int countMatches(String n_str, String d_str) {
        int count = 0;
        for (String c: n_str.split("")) {
            if( c.equals(d_str) ){
                count++;
            }
        }
        return count;
    }
 */
}
